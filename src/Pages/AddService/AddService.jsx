import React from "react";

const AddService = () => {
  return (
    <div className="max-w-4xl mx-auto">
      Create an Add Product page where there will be a form having the following
      fields: Image URL of the Service Service Name, Price, Service Area,
      Description Add button The person adding the service is a service
      Provider. You have to store service provider information with the service
      information, such as the Service provider image, the Service provider
      name, Service provider email will be taken from Firebase. On clicking the
      add service button, service information will be added in the database.
    </div>
  );
};

export default AddService;
